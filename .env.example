# ── 필수 ──────────────────────────────────
# 디스코드 개발자 포털 > Bot > Token
DISCORD_TOKEN=MTUyNTg3NzYyNDE4MDcwMzI5Mw.Gk9yUo.IvecmNwgBVIfK9sP9oZEKgQV37WDn-ayD8kTYo

# 디스코드 개발자 포털 > General Information > Application ID
CLIENT_ID=1525877624180703293

# ── 선택 ──────────────────────────────────
# 테스트할 서버 ID. 넣으면 그 서버에만 즉시 등록됨
# 비워두면 전역 등록 (모든 서버, 반영까지 최대 1시간)
GUILD_ID=1518970611357974650

# 봇 시작 시 명령어 자동 등록 (기본 켜짐, 바뀌었을 때만 등록)
# AUTO_DEPLOY=false
# FORCE_DEPLOY=true

# Render 슬립 방지용. Render 에서는 RENDER_EXTERNAL_URL 이 자동으로 들어오므로 비워둬도 됨
# SELF_URL=https://내앱이름.onrender.com
# KEEPALIVE_INTERVAL_MS=600000

# 로컬 테스트용 포트 (Render 는 자동 주입)
# PORT=3000
